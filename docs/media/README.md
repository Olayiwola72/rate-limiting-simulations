# Media Placeholders

This folder contains placeholder visuals used by the main `README.md`.

Recommended future files:

- `screenshot-home.png`
- `simulation-demo.gif`
- `token-bucket-demo.gif`
- `leaky-bucket-demo.gif`

You can keep the SVG placeholders during development and replace them later with real captures.
